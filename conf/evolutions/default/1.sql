# --- Created by Slick DDL
# To stop Slick DDL generation, remove this comment and start using Evolutions

# --- !Ups

create table "RECEIPTS" ("ID" INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1) NOT NULL PRIMARY KEY,"ACCOUNT_ID" INTEGER NOT NULL,"SUBMIT_DATE" TIMESTAMP NOT NULL,"EDIT_DATE" TIMESTAMP NOT NULL,"LOCATION" VARCHAR(64) NOT NULL,"CATEGORY" VARCHAR(64) NOT NULL,"PRICE" INTEGER NOT NULL,"RECEIPT_DATE" DATE NOT NULL,"NOTE" VARCHAR(512) NOT NULL);

# --- !Downs

drop table "RECEIPTS";

